-- local db_ok, _ = pcall(require, "dadbod-ui")
-- if not db_ok then
--   return
-- end

-- local map = require("user-util").map

-- map("<leader>db", "<Cmd>DBUI<cr>", { desc = "Open Database UI", mode = "n" })
