# override-meta

This allows you to specify new metadata for a package. It expects the following arguments in this order:

 - meta - A table of metadata to use to replace the metadata on the package supplied
 - package - The package to update

